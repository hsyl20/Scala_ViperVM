package fr.hsyl20.auratune.opencl

/*class Matrix[A](size:Long*) {
   private var _ready: Boolean = false

   def ready: Boolean = _ready

   def fill(a:A): Matrix[A] = {
      val m = new Matrix[A](size:_*)
      val k = new FillKernel[A]
      k(size)(m,a)
   }
}*/
